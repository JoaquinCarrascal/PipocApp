import { OrderTriggerPipe } from './order-trigger.pipe';

describe('OrderTriggerPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTriggerPipe();
    expect(pipe).toBeTruthy();
  });
});
